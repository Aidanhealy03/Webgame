# Webgame

A simple 3D mini-golf style browser game.

## Run locally

```bash
python3 -m http.server 8000
```

Then open `http://localhost:8000`.

## Controls

- Move ball: `WASD` or arrow keys
- Reset after scoring: `Space`
